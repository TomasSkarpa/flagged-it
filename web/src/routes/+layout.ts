// This tells SvelteKit to generate a static site with all pages prerendered
// and use client-side routing with a fallback for dynamic routes
export const prerender = true;
export const ssr = false;
